use "bag.mug";
# test instance call expression
class c {
	operator new() {
	}
	pub func new(): c {
		return c();
	}
	pub self func call(): ? {
	}
}
func main(args: str[]): ? {
	# calling static from class
	# calling instance function
	# c::new() calling static, store in buffer at compile time
	# call() calling instance onto the stack, store in buffer at compile time
	# ; end expression and copy buffer to expression body 

	c::new().call();
}