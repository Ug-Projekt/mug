use "shell.mug";
use "bag.mug";
model data {
	read name: str, # readonly
	age: i8,
}
func main(): ? {
	data d = data();
	io::println(d); # name: , age: 0
	d.age = 10;
	io::println(d) # name: , age: 10
	d = data("Carpal", 15) # no costructors
}