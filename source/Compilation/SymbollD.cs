using Mug.TypeSystem;
using System;

namespace Mug.Compilation
{/*
    public struct LiteralSymbolID : ISymbolID
    {
        public string Value { get; }

        public LiteralSymbolID(string path)
        {
            Value = path;
        }
    }

    public struct FunctionSymbolID : ISymbolID
    {
        public MugType BaseType { get; }
        public string Name { get; }
        public MugType[] Parameters { get; }

        public FunctionSymbolID(MugType basetype, string name, MugType[] parameters)
        {
            BaseType = basetype;
            Name = name;
            Parameters = parameters;
        }
    }

    public struct TypeSymbolID : ISymbolID
    {
        public string Name { get; }

        public TypeSymbolID(string name)
        {
            Name = name;
        }
    }

    public interface ISymbolID
    {
        abstract public bool Equals(object obj);

        abstract public string ToString();
    }*/
}
