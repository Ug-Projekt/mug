func main(): ? {
	var args: i32[] = {2,4,4,3};

	for i: i32 to args.len() {
		println("arg[", i, "]: ", args[i]);
	}
	var i: i32 = 0;
	while i < args.len() {
		println("arg[", i, "]: ", args[i]);
	}
}