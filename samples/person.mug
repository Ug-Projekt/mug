type Person {
	pub name: str,
	pub age: u8
}

func main(args: [str]): ? {
	const name: str = "someName";
	const age: u8 = 10;
	var person: Person = Person {
		name: name,
		age: age
	};
	// person destroied here
}